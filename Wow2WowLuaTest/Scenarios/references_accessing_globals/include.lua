
function runInclude()
    print("for A, include sees " .. tostring(A));
    print("for B, include sees " .. tostring(B));
    print("for I, include sees " .. tostring(I));
    print("for M, include sees " .. tostring(M));
end

runInclude();

I = "include"


